@model IEnumerable<ManufacturingTimeTracking.Controllers.ReportByWarehouseRow>
@{
    ViewData["Title"] = "Report by Warehouse";
}
<div class="animate-fade-in">
    <div class="mb-6">
        <a asp-action="Reports" class="inline-flex items-center text-slate-600 hover:text-slate-800 font-medium mb-3">← Back to reports</a>
        <h1 class="text-2xl font-bold text-slate-800">Report by Warehouse</h1>
        <p class="text-slate-500">Stock grouped by location.</p>
    </div>
    <div class="bg-white rounded-2xl shadow-soft border border-slate-100 overflow-hidden">
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-slate-200">
                <thead class="bg-slate-50/80">
                    <tr>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">Location</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">Zone</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">Total units</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">Lines</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">Items</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100">
                    @foreach (var r in Model)
                    {
                        <tr class="table-row-hover">
                            <td class="px-6 py-4 font-medium text-slate-800">@r.LocationCode</td>
                            <td class="px-6 py-4 text-slate-600">@r.Zone</td>
                            <td class="px-6 py-4 text-slate-600">@r.TotalQuantity</td>
                            <td class="px-6 py-4 text-slate-600">@r.LineCount</td>
                            <td class="px-6 py-4">
                                <ul class="space-y-0.5 text-sm text-slate-600">
                                    @foreach (var line in r.Items.OrderByDescending(x => x.Quantity).Take(20))
                                    {
                                        <li>@line.Sku — @line.Name: <strong class="text-slate-800">@line.Quantity</strong></li>
                                    }
                                    @if (r.Items.Count > 20)
                                    {
                                        <li class="text-slate-400">… and @(r.Items.Count - 20) more</li>
                                    }
                                </ul>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
