@page
@model ManufacturingTimeTracking.Pages.Materials.DeleteModel
@{
    ViewData["Title"] = "Delete Material";
}

<h1>Delete Material</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Material</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Material.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Material.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Material.Unit)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Material.Unit)
        </dd>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="Material.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a asp-page="./Index" class="btn btn-secondary">Back to List</a>
    </form>
</div>
