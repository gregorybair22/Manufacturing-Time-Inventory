@page
@model ManufacturingTimeTracking.Pages.Tools.DeleteModel
@{
    ViewData["Title"] = "Delete Tool";
}

<div class="animate-fade-in w-full">
    <h1 class="text-2xl font-bold text-slate-800 mb-2">Delete Tool</h1>
    <p class="text-slate-500 mb-6">Are you sure you want to delete this tool?</p>
    <div class="bg-white rounded-2xl shadow-soft border border-slate-100 p-6 mb-6">
        <dl class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div><dt class="text-sm font-medium text-slate-500">@Html.DisplayNameFor(model => model.Tool.Name)</dt><dd class="font-medium text-slate-800">@Html.DisplayFor(model => model.Tool.Name)</dd></div>
        </dl>
    </div>
    <form method="post" class="flex gap-3">
    <div class="m-auto">
        <input type="hidden" asp-for="Tool.Id" />
        <button type="submit" class="px-5 py-2.5 rounded-xl bg-red-600 text-white font-medium hover:bg-red-700 shadow-md transition-all duration-200">Delete</button>
        <a asp-page="./Index" class="px-5 py-2.5 rounded-xl border border-slate-300 text-slate-700 font-medium hover:bg-slate-50 transition-all duration-200">Back to List</a>
    </div>
    </form>
</div>
