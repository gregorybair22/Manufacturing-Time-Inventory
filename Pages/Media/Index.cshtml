@page
@model ManufacturingTimeTracking.Pages.Media.IndexModel
@{
    ViewData["Title"] = "Media";
}

<div class="animate-fade-in">
    <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-6">
        <h1 class="text-2xl font-bold text-slate-800">Media</h1>
        <a asp-page="Create" class="inline-flex items-center justify-center px-5 py-2.5 rounded-xl bg-blue-600 text-white font-medium hover:bg-blue-700 shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all duration-200 w-fit">Create New Media</a>
    </div>
    <div class="bg-white rounded-2xl shadow-soft border border-slate-100 overflow-hidden">
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-slate-200">
                <thead class="bg-slate-50/80">
                    <tr>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">Type</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">URL/Path</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">Uploaded By</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider">Uploaded At</th>
                        <th class="px-6 py-4 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100">
                    @foreach (var media in Model.MediaList)
                    {
                        <tr class="table-row-hover transition-colors duration-150">
                            <td class="px-6 py-4 font-medium text-slate-800">@media.Type</td>
                            <td class="px-6 py-4 text-slate-600 max-w-xs truncate" title="@media.UrlOrPath">@(media.UrlOrPath.Length > 50 ? media.UrlOrPath.Substring(0, 50) + "…" : media.UrlOrPath)</td>
                            <td class="px-6 py-4 text-slate-600">@media.UploadedBy</td>
                            <td class="px-6 py-4 text-slate-600">@media.UploadedAt.ToString("g")</td>
                            <td class="px-6 py-4 text-right">
                                <a asp-page="./Edit" asp-route-id="@media.Id" class="inline-flex items-center px-3 py-1.5 rounded-lg border border-blue-600 text-blue-600 text-sm font-medium hover:bg-blue-50 mr-2">Edit</a>
                                <a asp-page="./Delete" asp-route-id="@media.Id" class="inline-flex items-center px-3 py-1.5 rounded-lg border border-red-600 text-red-600 text-sm font-medium hover:bg-red-50">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
